import React from "react";
// import { createUseStyles } from "react-jss";

// const useStyles = createUseStyles((theme) => ({

// }));

const AppBar = (props) => {
    // const classes = useStyles();
    return (
        <div>
            <span>This is AppBar page</span>
        </div>
    );
}

export default AppBar;